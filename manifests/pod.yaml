apiVersion: v1
kind: Pod
metadata:
  name: nginx-firefox-pod
  labels:
    app: nginx-firefox
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      ports:
        - containerPort: 80
